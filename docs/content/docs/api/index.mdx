---
title: Overview
description: Remote apps talk to cellar-gateway over HTTPS
---

Apps talk to **`cellar-gateway`** over HTTPS (JSON). The gateway runs on manager or worker hosts, dials manager `SandboxAPI` with the cluster CA, and forwards each caller’s API key. Mint the key once with the CLI; do **not** run `cellar sandbox …` from application code.

Use the **official microsandbox SDKs** in cloud mode. Set the cloud backend URL to your Cellar gateway (for example `https://cellar.example.com`) and pass a Cellar API key.

<Cards>
  <Card title="API keys" href="/docs/api/api-keys" description="Create, list, and revoke keys" />
  <Card
    title="Run the gateway"
    href="/docs/api/gateway"
    description="Health checks and HTTP routes"
  />
  <Card
    title="AWS load balancer"
    href="/docs/api/load-balancer"
    description="ALB in front of gateway instances"
  />
  <Card
    title="Configure the client"
    href="/docs/api/configure"
    description="API key and gateway URL for msb SDKs"
  />
</Cards>
