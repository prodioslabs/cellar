# Isolated alpine sandbox (no external network).
# cellar-agent is injected as PID 1; use `cellar sandbox exec` to run commands.
# Usage: cellar sandbox create -f examples/sandbox.yaml
id: demo-alpine
image: alpine
