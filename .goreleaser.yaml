# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: cellar

before:
  hooks:
    - go mod tidy

builds:
  - id: cellar
    main: ./cmd/cellar
    binary: cellar
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
        -X github.com/prodioslabs/cellar/internal/version.Version={{.Version}}
        -X github.com/prodioslabs/cellar/internal/version.Commit={{.Commit}}
        -X github.com/prodioslabs/cellar/internal/version.Date={{.Date}}

  - id: cellard
    main: ./cmd/cellard
    binary: cellard
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
        -X github.com/prodioslabs/cellar/internal/version.Version={{.Version}}
        -X github.com/prodioslabs/cellar/internal/version.Commit={{.Commit}}
        -X github.com/prodioslabs/cellar/internal/version.Date={{.Date}}

  - id: cellar-gateway
    main: ./cmd/cellar-gateway
    binary: cellar-gateway
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
        -X github.com/prodioslabs/cellar/internal/version.Version={{.Version}}
        -X github.com/prodioslabs/cellar/internal/version.Commit={{.Commit}}
        -X github.com/prodioslabs/cellar/internal/version.Date={{.Date}}

  - id: cellar-agent
    main: ./cmd/cellar-agent
    binary: cellar-agent
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
        -X github.com/prodioslabs/cellar/internal/version.Version={{.Version}}
        -X github.com/prodioslabs/cellar/internal/version.Commit={{.Commit}}
        -X github.com/prodioslabs/cellar/internal/version.Date={{.Date}}

  - id: cellar-egress-gateway
    main: ./cmd/cellar-egress-gateway
    binary: cellar-egress-gateway
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
        -X github.com/prodioslabs/cellar/internal/version.Version={{.Version}}
        -X github.com/prodioslabs/cellar/internal/version.Commit={{.Commit}}
        -X github.com/prodioslabs/cellar/internal/version.Date={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    name_template: >-
      cellar_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    # cellar-agent and cellar-egress-gateway are linux-only (run inside containers);
    # darwin archives omit them.
    allow_different_binary_count: true
    files:
      - LICENSE
      - README.md
      - contrib/systemd/*
      - contrib/launchd/*

checksum:
  name_template: checksums.txt
  algorithm: sha256
  # Include prebuilt egress-gateway image archives in checksums.txt.
  extra_files:
    - glob: ./release-extras/*.tar.gz

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  prerelease: auto
  name_template: "v{{.Version}}"
  # Prebuilt cellar/egress-gateway image archives (docker save | gzip), produced
  # by the release workflow before `goreleaser release`.
  extra_files:
    - glob: ./release-extras/*.tar.gz
